import { FilterSelectTextPipe } from './filter-select-text.pipe';

describe('FilterSelectTextPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSelectTextPipe();
    expect(pipe).toBeTruthy();
  });
});
